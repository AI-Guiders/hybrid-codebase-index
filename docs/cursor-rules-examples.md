# Cursor: примеры правил (копипаст)

Этот репозиторий — индекс кода; готовые фрагменты для `.cursor/rules` лежат **в соседних субмодулях** метарепозитория `open` (после `git submodule update --init`):

| Тема | Файл-пример (относительно корня `open/`) |
|------|---------------------------------------------|
| Roslyn MCP, диагностики, sync namespaces | `roslyn-mcp/docs/cursor-rules-examples.md` |
| Отладка .NET через dotnet-debug-mcp | `dotnet-debug-mcp/docs/cursor-rules-examples.md` |
| Логические коммиты + git MCP | `git-mcp/docs/cursor-rules-examples.md` |
| Рефакторинг после проверки, C# 14 | `cascade-ide/docs/cursor-rules-examples.md` |

Публичный URL корня метарепо зависит от твоего зеркала (GitLab/GitHub); ориентир — тот же хостинг, откуда клонирован `open`.
